<view class="container" bind:tap="handleSelect">
	<i-badge show="{{showBadge}}" count="{{badgeCount}}">
		<i-icon
		 wx:if="{{icon}}"
		 name="{{icon}}"
		 color="#333333"
		 size="{{iconSize}}"
		/>
		<view>{{text}}</view>
	</i-badge>
</view>

