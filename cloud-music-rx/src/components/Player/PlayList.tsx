import React from 'react';

import s from './PlayList.module.less';

type PlayListProps = {

};

function PlayList({

}: PlayListProps) {
  return (
    <div></div>
  );
}

export default React.memo(PlayList);
